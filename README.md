# RemoveColor.com

English-first SEO tool site for removing a selected color from an image locally in the browser.

## Commands

```bash
npm install
npm run dev
npm test
npm run build
```

The V1 tool supports PNG, JPG, JPEG, and WebP input, then exports a transparent PNG.
